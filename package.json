{
  "name": "patterns",
  "version": "1.0.0",
  "description": "play on design patterns",
  "author": "tompascall",
  "license": "MIT",
  "private": false,
  "scripts": {
    "jest": "jest",
    "jest:watch": "jest --watch",
    "flow": "flow",
    "flow-watch": "fswatch -o ./ | xargs -n1 -I{} sh -c 'clear; printf \"\\033[3J\" && flow'",
    "build": "rm -f build && babel demo --out-dir dist/demo && babel src --out-dir dist/src"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "jest": "^22.4.3"
  },
  "keywords": [
    "design",
    "patterns"
  ],
  "devDependencies": {
    "babel-preset-flow": "^6.23.0",
    "fast-deep-equal": "^2.0.1",
    "flow-bin": "^0.76.0"
  }
}
